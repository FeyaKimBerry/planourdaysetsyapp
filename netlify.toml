[build]
  command = "npm run build"
  publish = "dist"

# The marketing homepage (index.html) is served at "/".
# The React app lives in login.html — serve it at the clean URL "/login".
[[redirects]]
  from = "/login"
  to = "/login.html"
  status = 200
